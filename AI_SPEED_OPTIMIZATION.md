# MindSetu AI speed optimization

This branch prepares the chatbot for native Ollama streaming and removes the frontend's artificial per-character delay.

The optimization workflow validates Python syntax and the Vite production build before committing the patched application files.
