{
  "name": "mindsetu-sih26",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "concurrently \"uvicorn sih26186_server:app --reload --port 8000 --app-dir backend\" \"npm --prefix frontend run dev\""
  }
}
